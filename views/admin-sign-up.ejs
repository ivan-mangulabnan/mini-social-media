<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Be an Admin</title>
  <link rel="stylesheet" href="./styles.css">
</head>
<body>
  <header class="header">
    <div class="header-content">
      <h1 class="logo">SMC</h1>
      <nav class="nav-buttons">
        <a href="/" class="nav-link-btn admin">
          Home
        </a>
      </nav>
    </div>
  </header>
  <div class="form-container">
    <h2 class="form-title">Admin Access Request</h2>
    <p class="form-subtitle">Enter the special verification code to request administrative privileges.</p>
      
    <% if (errors.length > 0) { %>
      <div id="login-error-display" class="error-display">
        <ul class="error-list">
          <% errors.forEach(error => { %>
            <li><%= error %></li>
          <% }) %>
        </ul>
      </div>
    <% } %>

    <form action="/admin-sign-up" method="post" class="form-layout">
      <div class="form-field">
        <label for="admin_code" class="form-label">Admin Verification Code</label>
        <input type="text" id="admin_code" name="code" required class="form-input" placeholder="e.g., secret">
      </div>
      <button type="submit" class="form-submit-btn form-submit-btn-admin">
        Submit Admin Code
      </button>
    </form>
      
    <p class="form-footer-link">
      <a href="/" class="form-link">
        ← Go back to home
      </a>
    </p>
  </div>
</body>
</html>